# SCRATCH ONLY (never merge): typescript-eslint 8.x peers on typescript <6.1.0,
# so npm ci refuses typescript 7 with ERESOLVE. This lets the proof run get
# past install to the build; lint is still expected to fail.
legacy-peer-deps=true
