STUDY-RK-067 Q3 Analogist — tip `f2e6d01`
Six hold-with-limit; two fail-transfer. Soft folklore invent JSON-only UX: 0. STUDY-RK-081 invented: 0. Concrete gap: no.

1. Command Line Interface Guidelines — humans first; --json opt-in — clig.dev — ongoing — https://clig.dev/ — Hold-with-limit: human-readable default; machine JSON when flagged. Limit: guide ≠ Commander; rejects invent JSON-only UX.

2. GitHub CLI: plain text default; --json fields + --jq — gh help formatting — ongoing — https://cli.github.com/manual/gh_help_formatting — Hold-with-limit: dual channel is mainstream; JSON is additive. Limit: field projection richer than tip emit(); same dual discipline.

3. AWS CLI --output json|text|table — AWS Docs — ongoing — https://docs.aws.amazon.com/cli/latest/userguide/cli-usage-output-format.html — Hold-with-limit: format is a switch; text remains first-class. Limit: AWS default json historically ≠ tip default text; tip text-default still matches human-first.

4. kubectl -o json / -o yaml alongside human tables — Kubernetes Docs — ongoing — https://kubernetes.io/docs/reference/kubectl/generated/kubectl_get/ — Hold-with-limit: -o selects machine view without deleting human get tables. Limit: k8s ≠ rk show; same dual pattern.

5. stdout data vs stderr diagnostics (TTY-aware) — clig.dev Output — ongoing — https://clig.dev/#output — Hold-with-limit: keep JSON on stdout clean for pipes. Limit: tip emit focuses on payload split; stderr polish optional not a gap.

6. Human table vs --json parallel views — openstatus CLI / clig — ongoing — https://www.openstatus.dev/blog/building-cli-for-human-and-agents — Hold-with-limit: different views, same facts. Limit: blog ≠ vendor RFC; supports KEEP dual.

7. Soft folklore invent JSON-only UX / invent remove-human-text / invent coding gap because text remains — Fail-transfer: Soft folklore: 0. Concrete gap = no. KEEP dual human+`--json`. No coding PR.

8. invent STUDY-RK-081 or treat scripts as obsolete for human output — Fail-transfer: do not invent STUDY-RK-081; STUDY-RK-038 stands.

Verifier: Analogist 1–6 HOLD · 7–8 FAIL-TRANSFER. Soft folklore: 0.

✅
