STUDY-RK-071 Q3 Analogist — tip `1a51052`
Six analogs listed; Verifier: 5 Hold · #6 UNVERIFIED · 7–8 Fail-transfer. Soft folklore invent auto-archive-default / 404=safe-prune: 0. STUDY-RK-081 invented: 0.

1. Docker prune prompts; volumes excluded by default — Docker Docs — ongoing — https://docs.docker.com/engine/manage-resources/pruning/ — Hold-with-limit: GC is opt-in/confirm; unused ≠ auto-delete. Limit: containers ≠ GH listing absence.

2. docker system prune -f only skips prompt when forced — Docker CLI reference — ongoing — https://docs.docker.com/reference/cli/docker/system/prune/ — Hold-with-limit: destructive path needs explicit force; tip uses --prune-vanished. Limit: force flag ≠ invent auto-archive-default.

3. K8s Job TTL-after-finished is opt-in field — Kubernetes Docs — ongoing — https://kubernetes.io/docs/concepts/workloads/controllers/ttlafterfinished/ — Hold-with-limit: cleanup only when ttlSecondsAfterFinished set. Limit: Job TTL ≠ repo sync.

4. S3 delete-marker / GET 404 ≠ permanent purge — AWS S3 — ongoing — https://docs.aws.amazon.com/AmazonS3/latest/userguide/DeletingObjectVersions.html — Hold-with-limit: apparent absence is not safe hard-delete. Limit: Soft folklore 404=safe-prune fails.

5. S3 NoncurrentVersionExpiration is separate lifecycle config — AWS S3 API — ongoing — https://docs.aws.amazon.com/AmazonS3/latest/API/API_NoncurrentVersionExpiration.html — Hold-with-limit: permanent removal requires explicit lifecycle rule. Limit: days-based expire ≠ tip vanished detect.

6. Soft-delete / purge jobs are scheduled opt-in — BMC CMDB soft-delete/purge — ongoing — https://docs.bmc.com/docs/cmdb/soft-delete-and-purge-of-cis-990713579.html — UNVERIFIED — flag; do not land as verified.

7. Soft folklore invent auto-archive-default / invent 404=safe-prune / invent empty-fetch=archive-all — Fail-transfer: Soft folklore: 0. KEEP pruneVanished default false. Concrete gap = no.

8. invent STUDY-RK-081 or treat detect-only sync as incomplete without default archive — Fail-transfer: do not invent STUDY-RK-081; STUDY-RK-034 stands.

Verifier: Analogist 5 Hold · #6 UNVERIFIED · 7–8 FAIL-TRANSFER.

✅
